# ===========================================
# pBTC Buyback & Distribution System
# Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values

# ----- SUPABASE -----
# Your Supabase project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase anon key (safe for client-side)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Supabase service role key (server-side only - KEEP SECRET)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ----- HELIUS (Solana RPC) -----
# Your Helius API key for Solana RPC and DAS API
HELIUS_API_KEY=your-helius-api-key

# Helius RPC URL (auto-constructed from API key if not set)
# HELIUS_RPC_URL=https://mainnet.helius-rpc.com/?api-key=your-api-key

# Helius Webhook Authentication Token (Bearer token for webhook endpoint)
# Set this to match the "Authentication Header" value in your Helius webhook config
# Format: Just the token value (without "Bearer " prefix)
HELIUS_WEBHOOK_AUTH_TOKEN=your-helius-webhook-bearer-token

# ----- TOKEN CONFIGURATION -----
# pBTC Token Mint Address
PBTC_TOKEN_MINT=your-pbtc-token-mint-address

# WBTC Token Mint Address (Wrapped Bitcoin on Solana - default provided)
WBTC_TOKEN_MINT=3NZ9JMVBmGAqocybic2c7LQCJScmgsAZ6vQqTDzcqmJh

# ----- DEV WALLET (For Buybacks) -----
# Private key for the dev wallet that claims creator rewards
# Format: base58 encoded private key OR JSON array of bytes
# Set this in DigitalOcean App Platform environment variables
DEV_WALLET_PRIVATE_KEY=

# ----- CRON SECURITY -----
# Secret token to authenticate cron job requests
# Generate with: openssl rand -hex 32
CRON_SECRET=

# ----- DIGITALOCEAN -----
# DigitalOcean API token (for deployments)
# DO_API_KEY=

